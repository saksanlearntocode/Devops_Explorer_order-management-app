apiVersion: v1
kind: Service
metadata:
  name: order-api-clusterip
  namespace: order-system
spec:
  type: {{ .Values.service.type }}
  selector:
    app: order-api
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 3000